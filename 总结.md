# 项目总结

## 1. 项目概述

本项目源于智慧停车场景中对车位检测系统高可用性的需求，聚焦地下车库弱光、电磁干扰、温湿度变化等复杂工况下的车位占用状态检测问题。系统基于三通道传感器（视觉摄像头、地磁传感器、超声波传感器），通过**环境自适应加权 D‑S 证据融合**算法实现多模态决策融合，在通道退化条件下有效维持检测精度。项目包含完整的信号仿真生成、独立检测器训练、融合决策和实验评估体系。

---

## 2. 数据仿真生成模块

### 2.1 占用事件序列生成（`sim_events.py`）

#### 数学原理

车位占用状态由**单车位交替更新过程**生成：空闲 → 到达 → 占用 → 离开 → 空闲……该过程严格保证单个车位同一时间最多被一辆车占用。

**到达过程**：采用非齐次泊松过程，时变到达率 $\lambda(t)$（辆/小时）为双峰高斯叠加基线模型：

$$
\lambda(t) = \lambda_{\text{base}} + A_1 \exp\left(-\frac{(t-8)^2}{2\sigma^2}\right) + A_2 \exp\left(-\frac{(t-18)^2}{2\sigma^2}\right)
$$

其中 $\sigma = 1.5$ h，$\lambda_{\text{base}} = 0.05$ 辆/小时，$A_1 = 0.2 A_2$（上午峰为晚高峰的 20%），周末模式整体乘 0.6。晚高峰峰值 $A_2$ 通过二分搜索自动标定，目标为平均占用率约 0.68–0.72。

**停车时长分布**：混合模型
- 长停（住户，70%）：$T \sim \text{LogNormal}(\mu=\ln(28800),\ \sigma=0.6)$ 秒（约 8 小时）
- 短停（访客，30%）：$T \sim \text{Exp}(\lambda=1/5400)$ 秒（约 1.5 小时），最小 60 秒

**算法流程**：
1. 基于 thinning 算法生成非齐次泊松到达时刻
2. 每次到达抽样停车时长，记录到达–离开区间
3. 将事件区间展开为 $f_s = 10$ Hz 的二值序列 $s(t)$

**关键参数**（`config.py`）：

| 参数 | 值 | 说明 |
|------|----|------|
| `FS` | 10 Hz | 全局采样率 |
| `SEED` | 42 | 全局随机种子 |

---

### 2.2 地磁通道仿真（`sim_geomag.py`）

#### 数学原理

地磁传感器（AMR 型）埋于地下约 5 cm，测量地球背景场与车辆磁扰动。

**背景场模型**（空闲状态）：

$$
\mathbf{B}_{\text{base}}(t_k) = \mathbf{B}_{\text{earth}} + \mathbf{d}_k + \mathbf{n}_k
$$

- 地球参考场 $\mathbf{B}_{\text{earth}} = (28.5, -1.5, 38.0)\ \mu\text{T}$（成都，由 IGRF-13 模型计算）
- 漂移 $\mathbf{d}_k$ 为随机游走：$\Delta \mathbf{d}_k \sim \mathcal{N}(0, \sigma_{\text{drift}}^2 \Delta t \cdot \mathbf{I}_3)$
- $\sigma_{\text{drift}} = 0.0017\ \mu\text{T}/\sqrt{\text{s}}$（对应约 0.1 μT/h 漂移速率）
- 传感器噪声 $\mathbf{n}_k \sim \mathcal{N}(0, 0.3^2 \mathbf{I}_3)\ \mu\text{T}$

**车辆扰动模型**（占用状态）：
车辆等效为磁偶极子，磁矩 $\mathbf{m}=m_0\hat{\mathbf{e}}$，$m_0\sim\text{TruncN}(2.0, 1.0^2, 0.3, 6.0)\ \text{A·m}^2$。方向限制在竖直方向 ±60° 内（$\cos\theta\ge0.5$）。传感器到偶极子中心距离 $|\mathbf{r}|\sim U(0.4, 0.8)$ m，方向垂直向下。

磁偶极场：

$$
\mathbf{B}_{\text{dip}} = \frac{\mu_0}{4\pi r^3}\Big[3(\mathbf{m}\cdot\hat{\mathbf{r}})\hat{\mathbf{r}} - \mathbf{m}\Big]
$$

其中 $\mu_0/4\pi = 10^{-7}\ \text{H/m}$，计算结果转换为 μT。

**物理约束保护**：
- 若 $m_0 < 1.0$ 且 $r > 0.6$ m，强制 $r \sim U(0.4, 0.6)$ m
- 若扰动 Z 分量幅度 $< 0.8\ \mu\text{T}$，等比放大至 $0.8\ \mu\text{T}$

**占用/空闲过渡**：指数平滑
- 占用：$\text{weight}_{\text{on}} = 1 - e^{-(t-t_{\text{on}})/\tau_{\text{on}}}$
- 空闲：$\text{weight}_{\text{off}} = e^{-(t-t_{\text{off}})/\tau_{\text{off}}}$
- $\tau \sim U(0.5, 2.0)$ s

#### 失效注入

| 失效类型 | 关键参数 | 算法 |
|---------|---------|------|
| EMI 干扰 | $A_{\text{total}}$（当前实验取 0–5 μT） | 谐波混叠 + 粉红噪声 + 随机脉冲 |
| 强漂移 | $\sigma_{\text{drift}}$ 倍率（1–100） | 增大随机游走步长 + 阶跃事件 |
| 邻位串扰 | 邻位距离 + 独立占用序列 | 几何衰减 $(0.5/r_i)^3$ + 投影修正 |
| 故障/丢包 | 故障类型 / 丢包率 | 垃圾值/卡死/离线/NaN |

**关键参数**（`config.py`）：

| 参数 | 值 | 说明 |
|------|----|------|
| `B_EARTH` | (28.5, -1.5, 38.0) μT | 成都参考地磁场 (北, 东, 下) |
| `SIGMA_N` | 0.3 μT | AMR 传感器噪声标准差 |
| `SIGMA_DRIFT` | 0.0017 μT/√s | 漂移强度 (≈0.1 μT/h) |
| `M0_MEAN/M0_STD` | 2.0/1.0 A·m² | 车辆磁矩分布参数 |
| `M0_MIN/M0_MAX` | 0.3/6.0 A·m² | 磁矩截断范围 |

---

### 2.3 超声通道仿真（`sim_ultrasonic.py`）

#### 数学原理

传感器安装于车位顶部 2.4 m 处。空闲时距离 $d = H = 2.4$ m；占用时 $d = H - h_{\text{top}}$，车顶高度 $h_{\text{top}}$ 按车型分布随机采样：

| 车型 | 占比 | 高度分布 |
|------|------|----------|
| 轿车 | 60% | $\mathcal{N}(1.45, 0.05^2)$ |
| SUV | 25% | $\mathcal{N}(1.65, 0.07^2)$ |
| 皮卡 | 8% | $\text{TruncN}(1.85, 0.10^2)$，上限 1.95 |
| 跑车 | 5% | $\mathcal{N}(1.30, 0.04^2)$ |
| 异形车 | 2% | $\mathcal{U}(1.95, 2.15)$ |

叠加高斯噪声 $\sigma_d = 0.02$ m。

#### 失效注入

**多径效应**：激活概率为 $1 - e^{-\text{severity}}$，生成连续事件序列。空闲时段被扰乱至 0.3–1.5 m 范围，占用时段被扰乱至 2.0–2.8 m，通过跨越判决阈值 1.85 m 制造错误分类。

**凝露/积灰**：添加均匀偏置 $b \sim U(-0.3, 0.3)$ m，噪声放大 $1.5\times \text{severity}$ 倍。

| 失效类型 | 实现方式 |
|---------|----------|
| 凝露/积灰 | 随机偏置 + 噪声放大 |
| 多径效应 | 连续事件序列跨越 1.85 m 阈值 |
| 丢包 | 以概率 $p$ 置 NaN |

**关键参数**（`config.py`）：

| 参数 | 值 | 说明 |
|------|----|------|
| `H_CEILING` | 2.4 m | 超声波安装高度 |
| `SIGMA_D_ULTRASONIC` | 0.02 m | 超声测距噪声 |

---

### 2.4 视觉通道退化注入（`sim_visual.py`）

使用 PKLot PUC 测试集（约 20 万张真实停车位图像）作为正常样本。退化注入在加载图像时动态执行：

**弱光模拟**（伽马校正）：

$$
I'(x) = 255 \cdot \left(\frac{I(x)}{255}\right)^{1/\gamma},\quad \gamma \in (0, 1]
$$

$\gamma < 1$ 时图像整体变暗。如 $\gamma=0.15$ 时实际指数约为 6.7，导致严重变暗。

---

## 3. 检测器设计（`detectors.py`）

### 3.1 地磁 SVM 检测器（`GeomagDetector`）

#### 特征工程
采用**因果 EMA 背景估计**机制：
- 背景向量 $\mathbf{B}_{\text{bg}}$ 仅在空闲时更新（指数移动平均，时间常数 $\tau = 600$ s），占用期间冻结
- 滑动窗口 $W = 100$ 个采样点（10 s），以窗口起始时刻的 $\mathbf{B}_{\text{bg}}$ 为参考
- 计算窗内每个采样点的 $\Delta B(t) = \|\mathbf{B}(t) - \mathbf{B}_{\text{bg}}\|$
- 提取特征向量：
  $$
  \mathbf{x} = \big[\text{mean}(\Delta B),\ \text{var}(\Delta B),\ \max(\Delta B)-\min(\Delta B)\big]
  $$
- 标签由窗口内多数投票决定

更新公式：
$$
\mathbf{B}_{\text{bg}}(t) = \alpha \mathbf{B}(t) + (1-\alpha)\mathbf{B}_{\text{bg}}(t-1),\quad \alpha = 1 - e^{-\Delta t / \tau}
$$

其中仅在 $s(t-1) = 0$（空闲）时更新，占用期间 $\mathbf{B}_{\text{bg}}(t) = \mathbf{B}_{\text{bg}}(t-1)$。

#### 分类器
RBF 核 SVM（`sklearn.svm.SVC`）：

$$
\min_{\mathbf{w},b} \frac{1}{2}\|\mathbf{w}\|^2 + C\sum_{i=1}^{n}\max(0, 1-y_i(\mathbf{w}^T\phi(\mathbf{x}_i)+b))
$$

$C = 1.0$，`probability=True` 通过 Platt 缩放输出占用概率 $P_m$。

**训练**：1 周正常工况仿真数据，均衡下采样至约 5000 样本，正常工况验收准确率 ≥ 0.95。

---

### 3.2 超声 Sigmoid 检测器（`UltrasonicDetector`）

**原理**：Sigmoid 软判决，将测距值 $d$ 映射为占用概率

$$
P_u(d) = \frac{1}{1 + \exp(-\alpha(d_{\text{th}} - d))}
$$

- $d_{\text{th}} = 1.85$ m（车顶高度阈值对应的距离）
- $\alpha$：斜率参数，控制器判决的软硬程度（当前实验中 $\alpha = 15.0$，由网格搜索优化确定）
- 缺失值填充为空位距离 2.4 m

---

### 3.3 视觉 MobileNetV3 检测器（`VisualDetector`）

**模型**：MobileNetV3-Small（ImageNet 预训练后 PKLot 微调），替换分类头为 2 输出（空闲/占用）

**预处理**：224×224 Resize → ToTensor → ImageNet 归一化

**训练**：PKLot UFPR04+UFPR05 训练集，UFPR04+UFPR05 验证集，测试使用 PUC 停车场

---

## 4. 环境自适应加权 D‑S 融合算法（EA‑DSF）

### 4.1 环境因子与权重映射

融合算法的核心思想：各通道在 BPA 构造中的权重由**实时环境因子**通过 Sigmoid/线性映射函数确定，使退化通道自动降权。

**视觉通道权重**（依赖于平均亮度 brightness）：

$$
w_v = \frac{1}{1 + \exp(-k_v \cdot (\text{brightness} - b_v))}
$$

其中 $k_v=15$，$b_v=0.2$。正常亮度（~0.5）时权重 ≈ 0.99，严重变暗时权重降至 0.1。

**超声通道权重**（依赖于多径概率 multipath_p）：

$$
w_u = \frac{1}{1 + \exp(k_u \cdot (\text{multipath\_p} - b_u))}
$$

其中 $k_u=10$，$b_u=0.45$。无多径时权重 ≈ 0.99，多径概率 ≥ 0.5 时权重显著降至接近 0.1。

**地磁通道权重**（依赖于 EMI 幅值）：

$$
w_m = \frac{1}{1 + \exp(k_m \cdot (\text{emi} - b_m))}
$$

其中 $k_m=0.5$，$b_m=10$（μT）。低干扰时权重 ≈ 0.99，EMI 达到 20 μT 时权重降至约 0.007。当前实验最大 EMI=5 μT，权重仍接近 1，为后续更高干扰实验预留了灵敏度。

**最小权重保护**：$w \leftarrow \max(w, 0.1)$，避免通道完全失效导致融合退化。

### 4.2 BPA 构造

识别框架 $\Theta = \{H_1\ (\text{occupied}), H_2\ (\text{free})\}$

对每个通道 $j \in \{v, m, u\}$：

$$
m_j(H_1) = P_j \cdot w_j,\quad m_j(H_2) = (1-P_j) \cdot w_j,\quad m_j(\Theta) = 1 - w_j
$$

### 4.3 冲突自适应组合

**冲突系数计算**：

$$
K_{\text{avg}} = \frac{1}{3}(K_{vm} + K_{vu} + K_{mu})
$$

其中 $K_{ij} = m_i(H_1) \cdot m_j(H_2) + m_i(H_2) \cdot m_j(H_1)$

**自适应冲突阈值**：

$$
\text{threshold} = 0.5 + 0.3 \cdot \exp(-0.5 \cdot (w_v + w_m + w_u))
$$

权重和越高，阈值越低，决策越积极。

**融合策略**：
- **低冲突**（$K_{\text{avg}} < \text{threshold}$）：标准 Dempster 顺序组合三个 BPA
- **高冲突**（$K_{\text{avg}} \ge \text{threshold}$）：归一化权重后增强最高权重通道话语权（×1.5），重新归一化后构造加权平均证据 $\bar{m}$，将 $\bar{m}$ 与自身组合两次

### 4.4 决策输出

**动态决策阈值**：

$$
\begin{aligned}
\text{threshold} &= 0.45 + 0.1 \cdot \Theta - 0.15 \cdot (1 - e^{-2K_{\text{avg}}}) + 0.1 \cdot e^{-2|H_1-H_2|} \\
\text{threshold} &\in [0.40, 0.65]
\end{aligned}
$$

- 若 $\text{Bel}(H_1) > \text{threshold}$：判定为占用
- 若 $\text{Bel}(H_2) > \text{threshold}$：判定为空闲
- 否则：**触发多数投票兜底**（三通道硬判决 ≥ 2 则占用）

---

## 5. 实验结果分析

### 5.1 多退化等级融合对比实验

**实验设置**：
- 7 个故障场景（v, m, u, vm, vu, mu, vmu——单/双/三通道失效）
- 5 个退化等级（0–4，见下表）
- 每组合 168 小时仿真（约 1008 个 10 分钟窗口）
- 对比方法：单通道（V, M, U）、Majority Vote (MV)、DS‑Equal、DS‑Static、Ours (EA‑DSF)

**退化参数设置**（本次实验）：

| 等级 | $\gamma$ | EMI (μT) | multipath_p（传入severity） |
|------|----------|----------|---------------------------|
| 0（无） | 1.0 | 0 | 0 |
| 1（轻） | 0.3 | 1.5 | 0.25 |
| 2（中） | 0.25 | 2 | 0.5 |
| 3（重） | 0.2 | 4 | 1 |
| 4（极） | 0.15 | 5 | 2 |

**主要发现**：

**（a）无退化等级（Level 0）**：单通道精度极高，MV 与 Ours 均达到 1.000，DS‑Equal 与 DS‑Static 略有下降（约 3 pp），总体差异微小。

**（b）轻度退化（Level 1）**：
- 场景 `v`（仅视觉退化至 79.4%）：Ours 保持 0.998，准确追踪地磁/超声的健康状态；DS‑Equal/DS‑Static 降至 77% 左右。
- 场景 `u`（仅超声轻微退化，仍达 98.9%）：所有方法均保持高精度（> 94.8%），超声轻微退化不足以引发严重冲突。
- 场景 `vu`（视觉 79.4%，超声 98.9%，地磁健康）：Ours 与 MV 接近 1.0，DS‑Equal 降至 77.4%，说明自适应权重的贡献：视觉降权后，剩两个高准确通道，融合仍可靠。

**（c）中度退化（Level 2）**：
- 场景 `vu`：超声降至 72.3%，DS‑Equal 暴跌至 63.3%，Ours 保持 0.997。差异达 36.4 pp，源于超声严重退化后与视觉/地磁高冲突，标准 D‑S 完全失效，而 EA‑DSF 通过降低超声权重并触发高冲突处理，成功隔离退化通道。
- 场景 `vmu`：三通道均明显退化（V=68.3%, M=74.9%, U=72.3%），Ours 准确率 76.9%，超最佳单通道 2.0 pp。经典 D‑S 仅 38.1%（-36.8 pp），说明在全面退化中 EA‑DSF 的鲁棒优势。

**（d）重度退化（Level 3）**：
- 场景 `mu`：超声降至 42.1%，DS‑Equal 崩溃至 0.9%（-98.1 pp），而 Ours 仍达 94.6%。OA‑DSF 的核心价值在此充分体现：冲突处理 + 多数投票兜底让单个极度退化通道无法摧毁全局决策。
- 场景 `vm`：视觉 56.0%+地磁 70.3%，Ours 准确率 98.5%，MV 为 96.0%。说明**即使两个通道退化，只要第三个足够健康（超声 100%），Ours 能自动放大健康通道权重并压制退化通道的证据**。

**（e）极重退化（Level 4）**：
- 超声在退化时降至 26.2–26.4%（极低），经典 D‑S 在三个含超声退化的场景中**完全失效（0.0%）**，Ours 依靠最小权重保护（0.1）和兜底机制仍维持：
  - `u`：99.8%（增益 +0.8 pp）
  - `vu`：99.5%（增益 +0.04 pp，几乎无损）
  - `mu`：97.3%（较最佳单通道 99.0% 仅损失 1.7 pp）
  - `vmu`：67.7%（超最佳单通道 70.2% 的 -2.5 pp，在不可能有好结果的情况下优雅降级）

**（f）各融合方法对比总结**：
- **MV**：简单快速，在退化通道占少数时表现好（如 `u` 场景），但退化通道占多数时（如双通道退化场景）随之下降；不存在因冲突导致的极端崩溃。
- **DS‑Equal**：低退化时正常，但一出现通道间明显矛盾即急剧失效（如 `u` Level 4 降至 0.0%）。这是标准 D‑S 规则的固有缺陷。
- **DS‑Static**：固定给视觉更高权重，在视觉退化时反效果（`vu` Level 2 仅 65.2%，比 DS‑Equal 63.3% 仅略好），表明静态权重不可靠。
- **Ours**：在所有场景下准确率始终高于或持平最佳单通道；冲突处理 + 自适应权重 + 兜底机制保证 EA‑DSF 在任何退化组合下均不崩溃，且在大部分场景下提供显著正增益。

---

### 5.2 消融实验

**实验设置**：
- 复用 H4 的 5 个退化等级 × 7 个场景
- 四个变体（见下表）

| 变体 | 权重 | 冲突处理 |
|------|------|----------|
| Full | 环境自适应 | 冲突自适应+多数投票兜底 |
| -Weight | 等权重 (1/3) | 同 Full |
| -Conflict | 环境自适应 | 始终标准 D‑S |
| -Adapt | 等权重 (1/3) | 始终标准 D‑S（即 DS‑Equal） |

**主要发现**：

**（a）自适应权重的贡献（Full vs -Weight）**：
- 在 `vm` Level 2：Full 0.986 vs -Weight 0.968（+1.8 pp）：自适应权重将超声权重推至接近 1，让健康通道主导融合。
- 在 `vmu` Level 4：Full 0.656 vs -Weight 0.416（+24 pp）：自适应权重给相对最健康的地磁最高信任，等权重则错误地将三个退化通道平等对待。

**（b）冲突处理的贡献（Full vs -Conflict）**：
- 在 `vu` Level 2：Full 0.994 vs -Conflict 0.509（+48.5 pp），是消融实验中**最大差距**。自适应权重降低了超声权重，但仅靠降权不足以解决冲突——必须配合冲突处理才能完全规避标准 D‑S 的缺陷。

**（c）等权重下冲突处理的贡献（-Weight vs -Adapt）**：
- 在 `u` Level 1：-Weight 0.967 vs -Adapt 0.094（+87.3 pp）。即使权重相等，冲突检测+Murphy 平均也能大幅优于直接标准 D‑S。这证明冲突处理是 D‑S 融合的必要组成部分。

**（d）完整方法相对朴素 D‑S 的提升（Full vs -Adapt）**：
- 在 `vmu` Level 1：Full 0.987 vs -Adapt 0.040（+94.7 pp）。自适应权重+冲突处理+兜底的联合作用，使极端退化条件下的可用性从完全失效恢复至接近正常水平。
- Level 0 所有场景：Full 与 -Adapt 仅差约 3 pp，证明正常条件下不引入回退。

**总结**：
1. **自适应权重**和**冲突自适应处理**是两个不可或缺的组件，单独使用其中任一个都不足以解决严重退化下的融合问题。
2. 在无退化或轻度退化条件下，EA‑DSF 与最佳单模态持平或略优，满足“不掉点”的设计要求。
3. 消融实验完整验证了论文中**环境自适应加权**和**冲突自适应 D‑S 融合**两个创新点的独立和协同贡献，为算法设计提供了清晰的因果链。